import React, { useContext, useEffect } from 'react'
import './Dashboard.css'
import { AdminContext } from '../../../context/AdminContext'

const Dashboard = () => {

  // const { authHeader, dashData, getDashData, } = useContext(AdminContext)

  // useEffect(() => {
  //   if (authHeader) {
  //     getDashData()
  //   }
  // }, [authHeader])

  // return dashData &&
  return (
    <div>

    </div>
  )
}

export default Dashboard
